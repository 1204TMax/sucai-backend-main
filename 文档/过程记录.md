# 过程记录

## 2026-05-10 创建工作流后台独立原型项目

- 所属环节：工作流后台原型创建
- 用户要求：
  - 在 `/Users/dashan/Documents/dev/sucai-backend-main` 创建独立后台项目。
  - 后台与 `sucai-main` 并列，不写入主站。
  - 后台包含登录页、主框架、工作流管理、工作流类型管理。
  - 后台需求文档、过程记录、设计文档独立维护。
  - 所有页面数据使用 mock，不做真实接口、真实 ComfyUI JSON 解析、真实映射或运行。
- 已完成：
  - 创建 `login.html` 登录页。
  - 创建 `index.html` 后台主框架。
  - 创建 `工作流管理.html`，包含节点列表、筛选、新建/编辑弹窗、mock 上传状态、输入项/输出项配置、上线/下线/删除。
  - 创建 `配置管理.html`，包含工作流类型列表、新增、编辑、删除。
  - 创建 `README.md`。
  - 创建 `文档/需求文档.md`。
  - 创建 `文档/设计文档.md`。
  - 创建 `共享样式/usage.md` 和 `共享样式/design-system.md`。
  - 创建 `共享样式/backend.css` 作为后台页面共享样式。
- 未修改：
  - 未修改 `/Users/dashan/Documents/dev/sucai-main`。

## 2026-05-10 调整 ComfyUI 输入输出配置方式

- 所属环节：工作流后台需求与原型调整
- 用户要求：
  - 上传 JSON 后，输入项和输出项需要通过“ComfyUI 节点 + 字段”的方式选择。
  - 输入项不需要默认值，不需要是否必填配置，默认都必填。
  - 输入项可以填写展示名称和描述。
  - 同一个封装节点内，输入项展示名称不允许完全一样。
  - 输出项可以选择 ComfyUI 节点和输出字段，并填写输出展示名称。
  - 保存时做校验；校验失败不允许保存，弹窗保留并展示错误信息。
- 已完成：
  - 更新 `文档/需求文档.md` 中新建节点、输入项、输出项和保存校验规则。
  - 更新 `工作流管理.html` 的新建/编辑节点弹窗。
  - 输入项改为 ComfyUI 节点下拉 + 输入字段下拉 + 展示名称 + 描述。
  - 输出项改为 ComfyUI 节点下拉 + 输出字段下拉 + 输出展示名称。
  - 移除输入项默认值和是否必填配置。
  - 增加输入项名称重复、输出项缺失等保存校验。

## 2026-05-10 需求文档字段规则细化

- 所属环节：工作流后台需求文档整理
- 用户问题：「需求文档，对每个字段的规则，都有所描述吗，有参考sucai-main的需求文档的写法吗」
- AI 做了什么：
  - 对照主站 `sucai-main` 工作流需求文档的表格化写法，补充后台需求文档中的字段级规则。
  - 将工作流管理列表字段、顶部筛选、新建节点基础字段、JSON 上传、输入项、输出项、工作流类型管理列表和类型表单改为表格说明。
  - 补清每个字段的展示规则、数据来源、交互方式和校验规则。

## 2026-05-10 根据产品审核意见补齐需求规则

- 所属环节：工作流后台需求文档完善
- 用户要求：
  - 不直接写入审核意见原文，而是根据审核意见整理成正式需求后写入需求文档。
  - JSON 文件限制为 100M，仅允许 `.json` 文件。
  - 上传失败或解析错误需要在上传区下方展示失败原因。
  - 允许替换 JSON；替换后匹配不到的输入/输出选择要清空，保存后才生效。
  - 新建节点默认下线，需要手动上线。
  - 节点删除或下线后，已发布工作流中该节点不可用，并提示“该节点被删除/下线，无法使用”；新任务运行到该节点时报错“XX节点：已被删除/下线”。
  - 节点名称不允许使用“审核”“开始”“结束”，提示“名称不合法，请更换”。
  - 使用中台账号登录，后台自行做权限管理，所有用户都可以查看。
  - 上线、下线、删除都需要确认和成功反馈。
- AI 做了什么：
  - 补充登录页和后台框架规则。
  - 补充节点操作确认、Toast 反馈、状态更新时间规则。
  - 补充 JSON 上传格式、大小、失败、替换和生效时机规则。
  - 补充字段类型映射、输出类型、输入项排序/删除和字数限制。
  - 补充节点名称非法值校验。
  - 调整任务快照规则，明确已开始任务与新开任务的边界。
  - 补充账号与权限口径。
  - 同步调整原型：工作流管理增加字段类型映射、输出类型展示、输入项删除、非法名称校验、上传失败展示、上线/下线确认与 Toast；工作流类型管理增加类型名称校验、重复校验和被节点使用时禁止删除。

## 2026-05-10 交互细节调整

- 用户提出：
  - 输入项区域删除“输入字段类型”独立展示。
  - 输出项区域不再展示原始字段类型。
  - JSON 上传区域改成文件上传组件，不使用进度条。
  - 节点描述改为两行高度。
  - 每个节点都可以上传一张 1:1 封面图。
- AI 做了什么：
  - 调整工作流管理原型：输入项只保留 ComfyUI 节点、输入字段、展示名称、描述和删除操作；字段类型仅作为内部解析结果保存。
  - 调整输出项区域：只保留 ComfyUI 节点、输出字段、输出类型和输出展示名称；输出字段内部类型仅用于系统解析和保存。
  - 调整 JSON 上传控件为文件选择组件，上传/解析错误继续展示在上传区域下方。
  - 调整节点描述 textarea 为两行高度。
  - 增加节点封面图上传控件、1:1 图片校验、列表封面展示和需求文档字段规则。
